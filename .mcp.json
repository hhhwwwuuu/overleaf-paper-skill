{
  "mcpServers": {
    "overleaf-git": {
      "command": "node",
      "args": ["./mcp-server/dist/index.js"],
      "env": {
        "OVERLEAF_GIT_TOKEN": "${OVERLEAF_GIT_TOKEN}"
      }
    }
  }
}
